# Can select ROS interfaces by interaction semantics

The learner correctly distinguished Topic for continuous one-way streams, Service for short request/response work, and Action for long-running work with feedback, result, and cancellation. They also created and successfully built all six VoiceNav Robot packages, so future lessons can assume basic workspace and package fluency and move on to custom interface generation.
